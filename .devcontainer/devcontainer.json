{
	"name": "Pure3270 Python 3.12 Development",
	"image": "mcr.microsoft.com/devcontainers/python:3.12-bullseye",
	"features": {
		"ghcr.io/devcontainers/features/github-cli": {},
		"ghcr.io/devcontainers/features/docker-in-docker": {
			"version": "latest",
			"enableNonRootDocker": "true"
		}
	},

	"runArgs": ["--memory=4g"],
	"containerEnv": {
		"DISPLAY": ":99",
		"DBUS_SESSION_BUS_ADDRESS": "unix:path=/var/run/dbus/system_bus_socket",
		"PUPPETEER_SKIP_CHROMIUM_DOWNLOAD": "true",
		"PUPPETEER_EXECUTABLE_PATH": "/usr/bin/chromium"
	},
	"postStartCommand": "echo 'vscode ALL=(ALL) NOPASSWD: /usr/bin/apt-get, /bin/tar, /usr/bin/curl, /bin/mv' | sudo tee /etc/sudoers.d/vscode",
	"postCreateCommand": "sh -c 'echo \"=== Starting postCreateCommand execution ===\" && echo \"Step 1: Installing Python dependencies...\" && pip install -e .[dev] || { echo \"ERROR: Failed to install Python dependencies\"; exit 1; } && echo \"Step 1 completed successfully\" && echo \"Step 2: Installing system dependencies...\" && sudo apt-get update && sudo apt-get install -y libdbus-1-3 libasound2 libatk1.0-0 libatk-bridge2.0-0 libc6 libdrm2 libgtk-3-0 libnss3 libx11-xcb1 libxcomposite1 libxdamage1 libxrandr2 xdg-utils chromium || { echo \"ERROR: Failed to install system dependencies\"; exit 1; } && echo \"Step 2 completed successfully\" && echo \"Step 3: Installing ACT v0.2.74...\" && set -e && echo \"Installing ACT v0.2.74\" && curl -LO https://github.com/nektos/act/releases/download/v0.2.74/act_Linux_x86_64.tar.gz && curl -LO https://github.com/nektos/act/releases/download/v0.2.74/checksums.txt && grep act_Linux_x86_64.tar.gz checksums.txt | sha256sum -c - && sudo tar -xzf act_Linux_x86_64.tar.gz -C /usr/local/bin act && rm act_Linux_x86_64.tar.gz checksums.txt && command -v act >/dev/null 2>&1 && echo \"ACT installation completed successfully\" && echo \"Step 3 completed successfully\" && echo \"=== All postCreateCommand steps completed successfully ===\"'",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.flake8",
				"ms-python.black-formatter",
				"ms-python.isort",
				"ms-python.mypy-type-checker"
			],
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python",
				"python.terminal.activateEnvironment": false
			}
		}
	}
}
