{
  "trace_file": "ibmlink.trc",
  "description": "IBM Link trace with screen buffer validation. F1 AID (Enter key) clears the screen per s3270 semantics; the trace ends with F1+UNBIND, so the final screen state is empty (0 fields).",
  "screen": {
    "rows": 24,
    "cols": 80,
    "num_fields": 0,
    "field_details": []
  },
  "validation_checks": [
    {
      "type": "screen_size",
      "expected": "24x80",
      "description": "Standard 3270 screen size"
    },
    {
      "type": "field_count",
      "min": 0,
      "max": 0,
      "description": "F1 AID clears the screen per s3270 semantics (TN3270E traces end with F1+UNBIND)"
    },
    {
      "type": "parsing_errors",
      "expected_errors": false,
      "description": "Should process without errors"
    }
  ]
}
